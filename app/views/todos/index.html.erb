<h1>Todos List</h1>
<%= link_to 'New Item', new_todo_path %>
<% @todos.each do |todo| %>
  <h2><%= todo.title %></h2>
  <%= link_to 'Show Todo Item', todo_path(todo) %>
  <%= link_to 'Edit Item', edit_todo_path(todo) %>
  <%= link_to 'Finished Task', todo, method: :delete %>
  <% end %>
