<h1>Todos List</h1>
<%= link_to 'New Item', new_todo_path %>
<% @todos.each do |todo| %>
  <h2><%= todo.title %></h2>
  <%= link_to 'Show List', todo_path(todo) %>
  <%= link_to 'Edit Item', edit_todo_path(todo) %>
  <% end %>
